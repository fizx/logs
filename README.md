A (so far) very simple logging package for golang.  Go's default logger doesn't have log levels.  This adds log levels:

- DEBUG
- INFO
- WARN
- ERROR

Also, I only care about the root logger.  Deal with it (or pull request).